# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                    controllers.Application.index

# VPN setup page
GET        /vpn                 controllers.VpnController.vpn

# VPN setup page
GET        /vpn/servers         controllers.VpnController.servers

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file        controllers.Assets.at(path="/public", file)

# VPN connection location
GET        /vpn/location        controllers.VpnController.location

# Spike VPN connection
GET        /vpn/connect         controllers.VpnController.connect

# Spike ping
GET        /vpn/ping            controllers.VpnController.ping

# Spike sudo
GET        /vpn/userName         controllers.VpnController.userName

